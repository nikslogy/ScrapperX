# ============================================
# ScrapperX Backend Configuration
# ============================================
# IMPORTANT: Copy this to .env and fill in your values
# NEVER commit .env to version control!

# ============================================
# Environment Settings
# ============================================
# Options: development, production
NODE_ENV=production

# Server port
PORT=5000

# ============================================
# CORS & Frontend
# ============================================
# Your frontend URL (REQUIRED for production)
FRONTEND_URL=https://your-domain.com

# Additional allowed origins (comma-separated)
# ALLOWED_ORIGINS=https://app.your-domain.com,https://admin.your-domain.com

# Trust reverse proxy headers (set to true if behind Nginx/Cloudflare)
TRUST_PROXY=true

# ============================================
# API Key Configuration (Hybrid Approach)
# ============================================
# 
# HOW IT WORKS:
# - Anonymous users: Limited access (3 scrapes/hour, 1 batch/hour)
# - API Key users: Full access (20 scrapes/min, 5 batches/5min)
# 
# This lets anyone TRY your API, but real usage needs a key.
# 
# Generate API keys with: npm run generate-key
# Or: node -e "console.log('scx_' + require('crypto').randomBytes(24).toString('base64url'))"
#
API_KEYS=scx_your-key-1,scx_your-key-2

# ============================================
# Rate Limits (Built-in, no config needed)
# ============================================
# 
# PRODUCTION LIMITS:
# ┌─────────────────┬───────────────────┬──────────────────────┐
# │ Endpoint        │ Anonymous         │ With API Key         │
# ├─────────────────┼───────────────────┼──────────────────────┤
# │ Quick Scrape    │ 3 requests/hour   │ 20 requests/minute   │
# │ Batch Scrape    │ 1 request/hour    │ 5 requests/5min      │
# │ Domain Crawler  │ ❌ Disabled       │ 3 requests/5min      │
# └─────────────────┴───────────────────┴──────────────────────┘
#
# DEVELOPMENT LIMITS: Much higher (50-100x) for testing

# ============================================
# Optional: Database (for crawler persistence)
# ============================================
# If not provided, crawler will work but won't persist sessions
# 
# MongoDB Atlas (free tier): 
#   Get connection string from https://mongodb.com
# 
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/scrapperx

# ============================================
# Optional: CAPTCHA Solving Services
# ============================================
# Get API keys from:
# - 2captcha: https://2captcha.com
# - AntiCaptcha: https://anti-captcha.com
# TWOCAPTCHA_API_KEY=your-2captcha-api-key-here
# ANTICAPTCHA_API_KEY=your-anticaptcha-api-key-here
